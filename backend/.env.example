# Cadena de conexión a SQL Server (formato Prisma)
DATABASE_URL="sqlserver://HOST:1433;database=contaassist;user=USUARIO;password=CONTRASENA;trustServerCertificate=true"

# Servidor
PORT=4000

# Origen permitido para CORS (frontend)
CORS_ORIGIN="http://localhost:5173"

# Autenticación
JWT_SECRET="CAMBIAR_POR_UN_SECRETO_LARGO_Y_ALEATORIO"
JWT_EXPIRES_IN="15m"
